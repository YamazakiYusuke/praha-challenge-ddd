export enum EnrollmentStatusValue {
  Enrolled,
  OnLeave,
  Withdrawn,
}

export enum AssignmentProgressStateValue {
  NotStarted,
  InReview,
  Completed,
}

export enum EmailStatus {
  Pending,
  Sending,
  Sent,
  Error,
}
